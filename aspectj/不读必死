1. 因为lombok和aspectj都有字节码生成过程，所以这俩不太兼容，但是只要搞一个没有lombok的jar包就可以了,也就是这个包，也要使用aspectj插件
<plugin>
    <groupId>org.codehaus.mojo</groupId>
    <artifactId>aspectj-maven-plugin</artifactId>

3. 同时在入口starter包里面aspectj的插件要指定好，切面jar包，也就是这个模块，同时指定好待植入的包，当然可以是你的内部包，也可以是其他房包，这个功能十分好
 <plugin>
     <groupId>org.codehaus.mojo</groupId>
     <artifactId>aspectj-maven-plugin</artifactId>
     <configuration>
         <aspectLibraries>
             <dependency>
                 <groupId>com.stellariver.meeting</groupId>
               <artifactId>aspectj</artifactId>
             </dependency>
         </aspectLibraries>
         <weaveDependencies>
             <dependency>
                 <groupId>com.stellariver.meeting</groupId>
                 <artifactId>application</artifactId>
             </dependency>
         </weaveDependencies>
     </configuration>
 </plugin>